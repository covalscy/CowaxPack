package com.cowax.cowaxpack.mixin;

/**
 * Исторический файл для отладки LOD в старых версиях SuperbWarfare.
 *
 * В текущей версии намеренно не используется как mixin, чтобы не создавать
 * предупреждения Mixin AP при сборке.
 */
public class VehicleModelMixin {
}
